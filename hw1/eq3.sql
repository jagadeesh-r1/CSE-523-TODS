SELECT LATITUDE, LONGITUDE, ZIPCODE, COUNT(*) AS COLLISION_COUNT FROM CSE532.COLLISION_ALL WHERE LATITUDE IS NOT NULL AND LONGITUDE IS NOT NULL AND ZIPCODE IS NOT NULL GROUP BY LATITUDE, LONGITUDE, ZIPCODE ORDER BY COLLISION_COUNT DESC 
FETCH FIRST 10 ROWS ONLY;